"""
Helps construct the standard data types declared in the
`event_standard_data_types.md` document.

These are very common structures yet are incompatible with each other
by the very nature of how Petronia generates the data structure objects.
One fix would be to have the generation create shared data types, but those would
still be incompatible between extensions.
"""
