
"""
Layout configuration API describes a common set of events used by the
implementations of the layout API to control their configuration.  Without
this layer, the implementations can use any hodge podge setup that will
prevent other extensions from working well with the layout in a general
way.
"""
